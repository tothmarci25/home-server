apiVersion: v1
kind: ConfigMap
metadata:
  name: vault-policies
  namespace: vault
  annotations:
    argocd.argoproj.io/sync-wave: "0"
data:
  cloudflared.hcl: |
    path "secret/data/cloudflared/*" {
      capabilities = ["read"]
    }
