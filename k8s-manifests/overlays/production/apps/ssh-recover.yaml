apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ssh-recover
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/tothmarci25/home-server
    targetRevision: main
    path: k8s-manifests/infrastructure/base/ssh-recover
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
